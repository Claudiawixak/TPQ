//10. Ordenação de um Array (Método de Bolha):
//Desenvolva um programa que leia 10 números inteiros e armazene-os em um array. Ordene os elementos do array em ordem crescente utilizando o método de ordenação
//por bolha (bubble sort) e imprima o array ordenado.
#include <stdio.h>

int main() {
    // Declaração das variáveis
    

    // Peça ao usuário ...
    printf("");
    scanf("%d", &variavel);
    
   
     printf("");
    scanf("%d", &variavel);

    //Execução

    printf("\nO resultado eh: %d\n", variavel);

    return 0;
}