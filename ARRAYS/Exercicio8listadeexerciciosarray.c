//8. Contagem de Frequências:
//Desenvolva um programa que leia 10 números inteiros e armazene-os em um array. O
//programa deve então contar quantas vezes cada número aparece no array e exibir essa
//frequência

#include <stdio.h>

int main() {
    // Declaração das variáveis
    int numerosInteiros[10], frequencia;

    // Peça ao usuário ...
    for (int i = 0; i < 10; i++)
    {
    printf("\nInforme o numero: %d\n");
    scanf("%d", &numerosInteiros[i]);
    }

    for (int i = 0; i < 10; i++)
    {
       int count = 1;
    }
    
    
    
    
   
     printf("");
    scanf("%d", &variavel);

    //Execução

    printf("\nO resultado eh: %d\n", variavel);

    return 0;
}