
#include <stdio.h>
//9. Remover Duplicatas de um Array:
//Escreva um programa que leia 10 números inteiros e armazene-os em um array. O
//programa deve remover quaisquer números duplicados do array e imprimir o array
//resultante.
int main() {
    // Declaração das variáveis
    

    // Peça ao usuário ...
    printf("");
    scanf("%d", &variavel);
    
   
     printf("");
    scanf("%d", &variavel);

    //Execução

    printf("\nO resultado eh: %d\n", variavel);

    return 0;
}